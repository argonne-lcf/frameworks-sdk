# vLLM XPU requirements — intended to be used alongside requirements-pytorch-triton-torchvision.txt
# Sources: vllm/requirements/common.txt, vllm/requirements/xpu.txt, vllm-xpu-kernels/requirements.txt
# Packages already fully covered by that base file are omitted here (cmake, ninja, numpy, wheel,
# pyyaml, psutil, pyelftools, pybind11, build[uv], lit, etc.)
#
# Constraints that differ from the base file are updated here and noted accordingly.
# Install base file first, then this file.
#
# Extra index for XPU torch builds:
# --extra-index-url=https://download.pytorch.org/whl/xpu
#
# ── Shared constraint updates (stricter than base file) ───────────────────────
setuptools>=77.0.3,<80.0.0   # narrows base (>=70.2.0,<82); vllm needs <81.0.0, kernels needs <80.0.0 → intersection
requests>=2.26.0              # adds floor to base file's unconstrained entry
filelock>=3.16.1              # adds floor; https://github.com/tox-dev/filelock/pull/317
jinja2>=3.1.6                 # adds floor to base file's unconstrained entry
packaging>=24.2               # adds floor to base file's unconstrained entry
six>=1.16.0                   # adds floor; transitive dep of pandas, safe to apply universally
typing_extensions>=4.15.0     # base file has >=4.15.0 (hyphen form); underscore alias listed for vllm compat
#
# ── Formatting / Dev tools ────────────────────────────────────────────────────
pre-commit==4.0.1                        # formatting (vllm-xpu-kernels)
#
# ── Testing ───────────────────────────────────────────────────────────────────
pytest                                   # test runner (vllm-xpu-kernels)
#
# ── vLLM common dependencies ──────────────────────────────────────────────────
regex                                    # higher-performance re replacement
cachetools
sentencepiece                            # required for LLaMA tokenizer
tqdm
blake3
py-cpuinfo
transformers>=4.56.0,<5
tokenizers>=0.21.1                       # required for fast incremental detokenization
protobuf>=5.29.6,!=6.30.*,!=6.31.*,!=6.32.*,!=6.33.0.*,!=6.33.1.*,!=6.33.2.*,!=6.33.3.*,!=6.33.4.*  # CVE-2026-0994
fastapi[standard]>=0.115.0               # form models in OpenAI API server audio transcriptions endpoint
aiohttp>=3.13.3
openai>=2.0.0                            # Responses API with reasoning content
pydantic>=2.12.0
prometheus_client>=0.18.0
prometheus-fastapi-instrumentator>=7.0.0
tiktoken>=0.6.0                          # required for DBRX tokenizer
lm-format-enforcer==0.11.3
llguidance>=1.3.0,<1.4.0; platform_machine == "x86_64" or platform_machine == "arm64" or platform_machine == "aarch64" or platform_machine == "s390x" or platform_machine == "ppc64le"
diskcache==5.6.3                         # required for outlines backend disk cache
lark==1.2.2
xgrammar>=0.1.32,<1.0.0; platform_machine == "x86_64" or platform_machine == "aarch64" or platform_machine == "arm64" or platform_machine == "s390x" or platform_machine == "ppc64le"
partial-json-parser                      # parsing partial JSON outputs
pyzmq>=25.0.0
msgspec
gguf>=0.17.0
mistral_common[image]>=1.10.0
opencv-python-headless>=4.13.0           # required for video IO
einops                                   # required for Qwen2-VL
compressed-tensors==0.14.0.1             # required for compressed-tensors
depyf==0.20.0                            # profiling and debugging with compilation config
cloudpickle                              # allows pickling lambda functions in model_executor
watchfiles                               # monitor TLS file updates in http server
python-json-logger                       # logging as per examples/others/logging_configuration.md
pybase64                                 # fast base64 implementation
cbor2                                    # cross-language serialization of hashable objects
ijson                                    # mistral streaming tool parser
setproctitle                             # set process names for debugging and monitoring
openai-harmony>=0.0.3                    # required for gpt-oss
anthropic>=0.71.0
model-hosting-container-standards>=0.1.13,<1.0.0
mcp
opentelemetry-sdk>=1.27.0
opentelemetry-api>=1.27.0
opentelemetry-exporter-otlp>=1.27.0
opentelemetry-semantic-conventions-ai>=0.4.1
#
# ── vLLM XPU-specific dependencies ───────────────────────────────────────────
ray>=2.9
setuptools-scm>=8
datasets                                 # for benchmark scripts
numba==0.61.2                            # required for N-gram speculative decoding
matplotlib                               # benchmark scripts, introduced by triton (vllm-xpu-kernels)
pandas                                   # benchmark scripts, introduced by triton (vllm-xpu-kernels)
#
